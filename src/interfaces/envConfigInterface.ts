export interface EnvConfigInterface {
  POSTGRES_PASSWORD: string;
  PGHOST: string;
  PGUSER: string;
  PGDATABASE: string;
  PGPASSWORD: string;
  PGPORT: number;
  PORT: number;
  JWT_SECRET_KEY: string;
}
